-#
-# locals:
-#   account
-#

%ul.nav.navbar-nav.navbar-right
  %li.dropdown
    = link_to "#{UserDecorator.decorate(current_user)} <span class='caret'></span>".html_safe, '#',
      :class => 'dropdown-toggle',
      :data => {:toggle => 'dropdown'}
    %ul.dropdown-menu
      %li= link_to 'My Account', [account, :my_account]
      %li= link_to 'Log out', destroy_user_session_path, :method => :delete
