.page-header
  .pull-left
    %h1 Administrator
  .pull-right
    = link_to 'Edit', [:edit, :admin, @admin],
      :class => 'btn btn-primary'
    = link_to 'Delete', [:admin, @admin],
      :method => :delete,
      :data => {:confirm => 'Are you sure?'},
      :class => 'btn btn-danger'
    = link_to 'Back', [:admin, :admins],
      :class => 'btn btn-default'

  .clearfix

- admin = @admin.decorate
.col-md-8
  %dl.dl-horizontal
    %dt Name
    %dd= admin

    %dt Email
    %dd= admin.email

    %dt Status
    %dd= admin.status

.col-md-4
  - unless @admin.confirmed?
    = link_to 'Re-send confirmation email', '#',
      :class => 'btn btn-link'

  - if @admin.id != current_user.id
    = form_for @admin,
      :url => [:reset_password, :admin, @admin],
      :method => :put do |f|

      = f.submit 'Send reset password instructions', :class => 'btn btn-link'

    = form_for @admin, :as => :user,
      :url => [:enable, :admin, @admin],
      :method => :put do |f|

      - submit_text = 'Enable'
      - value = false

      - if @admin.active?
        - submit_text = 'Disable'
        - value = true

      = f.hidden_field :disabled, :value => value
      = f.submit "#{submit_text} Account", :class => 'btn btn-link'
