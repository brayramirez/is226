-#
-# locals:
-#   orders
-#

.panel.panel-primary
  .panel-heading Open Orders with bids made

  - if orders.present?
    %table.table.table-condensed.table-striped.table-hover
      %thead
        %tr
          %th Item
          %th Buyer
          %th Created At
      %tbody
        - orders.each do |order|
          %tr
            %td= link_to order, [:bidder, order]
            %td= order.user
            %td= order.created_at
  - else
    .panel-body
      You have not made any bids yet.
      Check orders #{link_to 'here', [:bidder, :orders]}.
