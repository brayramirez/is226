.col-md-12
  .page-header
    %h1 Edit Bid

  = form_for @form, :url => [:bidder, @bid],
    :html => {:class => 'form-horizontal'} do |f|

    = render :partial => 'bidder/bids/partials/form',
      :locals => {:f => f, :order => @bid.order}
