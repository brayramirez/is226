.navbar.navbar-inverse.navbar-static-top
  .container
    .navbar-header
      = link_to 'IS226', [:buyer, :root], :class => 'navbar-brand'

    %ul.nav.navbar-nav.navbar-left
      %li.dropdown
        = link_to "Orders <span class='caret'></span>".html_safe, '#',
          :class => 'dropdown-toggle',
          :data => {:toggle => 'dropdown'}
        %ul.dropdown-menu
          %li= link_to 'Open', '#'
          %li= link_to 'Awarded', '#'
          %li= link_to 'Closed', '#'
          %li= link_to 'All', [:buyer, :orders]


    = render :partial => 'shared/my_account_nav',
      :locals => {:account => :buyer}
